# HANDOFF — start here in a new session

You are picking up the WG13 world-generator **engine** (attempt #13; `D:\world gen 13`). This doc transfers the *operating state and judgment*, not the architecture — the canonical docs hold that. Read this, then the two short docs in step 1, then act. Do **not** binge-read everything; that bloats context and degrades you as badly as starting cold.

> **Note on you:** session-to-session is the fragile transition. The previous sessions had a good rhythm the user wants kept ("the magic"). The way to keep it is not to absorb maximum context — it's to adopt the *posture* below and trust the gates. When unsure, do the methodical thing, not the clever thing.

## 1. Orient (read these two, in order — ~5 min, not everything)
1. `02_WORKFLOW.md` §8 (the working method) and §9 (engine-not-a-game). **This is the posture. Internalize it before touching anything.**
2. `PROGRESS.md` (where we are) + the **top** of `DRIFT_LOG.md` (most recent entries — what just happened, what's parked).

Then, only if you need them: `00_ARCHITECTURE.md` (the rules), `04_CODE_MAP.md` (what runs what + how to run a gate), `MILESTONE_1_land.md` / `MILESTONE_2_biomes.md` (the current step's detail). `README.md` lists full read-order. **Don't read the WG10 deep dive unless a step references it** — it's reference-only and long.

## 2. The posture (the part that's easy to lose)
- **Quality #1 = "do it right, no slop"** > Survivability > Modularity > Performance. **Apply the pillars yourself and state the decision.** Don't ask the user to ratify pillar calls — they've said this repeatedly. Surface only *genuine forks* (real design tradeoffs).
- **One gated step at a time.** Explain (plain language) → implement → **verify with evidence** (test PASS from stdout, or a measurement — never an eyeballed "it works") → update docs → commit at green → continue.
- **Systematic debugging always. If a fix doesn't work, REVERT it — never stack a second fix.** Question the measurement before trusting a number (we've been burned by `Performance.TIME_PROCESS`/`TIME_FPS` hiding spikes — use true per-frame `delta`, vsync off).
- **This is an engine, not a game.** No game-specific content in core; variability lives in data (`WorldConfig`, assets).
- **You launch scenes for the user** via `run.ps1` (PS Job → visible window). They only open the editor to tune in the inspector or when a Rust rebuild needs the DLL-lock released.
- **Use the brainstorming/debugging/etc. skills** when they apply (the harness reminds you). They've been load-bearing here.

## 3. Current state (update this in every handoff refresh) - refreshed 2026-06-06 (M2.4b scaffold scale review)
- **On `main` after backing out failed M2.4a** (`github.com/aarkan1313/g13`; run `git log -1 --oneline` for the exact hash). `m1-complete` tagged. M2.3 code checkpoint was `4a4b29d`; M2.4a failed visually twice and is preserved at branch/tag `backup/m2-4-failed-2026-06-06`.
- **Done: M1 COMPLETE + M1.9 perf hardening + M2.1 climate + M2.2 biome id + M2.3 composition-machine terrain structure.** M1 = streaming shell (bounded pool, annulus clipmap, never-black, no z-fight), LOD ~49 km, near-page collision, perf-hardened. M2.1 = temp/moisture climate. M2.2 = Whittaker biome id (10-biome roster, stats-based), visible with V -> biome mode. M2.3 = one general composition machine for terrain macro structure: uplift places ranges/hills vs flat lowlands, ridged/value relief adds mass, valley carve separates ranges, hand-set character constants for now.
- **M2.3 VISUAL PASS 2026-06-06.** User flew the live AABB-fixed world and said the terrain "looks really good" and is in a good spot for an early mountain/plain world. Fresh gates before close: `m2_3_composition_check` PASS (spread 0.81, no-cliff 5.1m), `m2_1_climate_check` PASS, `m2_2_biome_check` PASS, `m1_7c_stand_check` PASS.
- **Important distinction:** yes, the project has biomes already (M2.2 classifier/color skin). No, M2.3 has not done heavy DEM work yet, and terrain shape is not per-biome yet. The current mountains/plains come from the general M2.3 uplift composition. DEM scalar tuning was tried in M2.4a and failed visually.
- **CURRENT / NEXT STEP: M2.4b - DEM STRUCTURAL SCAFFOLD.** Use plan `docs/superpowers/plans/2026-06-06-m2-4b-dem-structural-scaffold.md` and provenance record `docs/superpowers/plans/2026-06-06-m2-4b-wg10-port-record.md`. The WG10 mountain synthesis lesson is: explicit range/channel/pass facts first, height assembly second, visual promotion last. Step 2/3 prototype now exists in `rust/structural_scaffold`: deterministic CPU-only RegionFacts plus `wg-13/_captures/m2_4b_scaffold_review.png/.md` and exported Godot review data. After the first bad toy-line sheet, the prototype was rewritten toward the actual WG10 recipe shape: domain-warped range envelopes, massif/base fields, primary+tributary carve masks, pass floors, and ridged/detail residuals. Static 3D review now has two scenes: `wg-13/scenes/m2_4b_scaffold_3d_review.tscn` (macro/concept: promising structure, too compressed/unplayable as gameplay scale) and `wg-13/scenes/m2_4b_scaffold_playable_scale_review.tscn` (single-panel scale-calibration view). Gate `m2_4b_scaffold_3d_check.gd` PASS covers both rendered scenes. It is NOT runtime-integrated and NOT M2.4 complete. UPDATE (2026-06-06): the GPU-portable per-cell oracle (`sample_cell`) was rendered for the first time (oracle review scene + `--source` CLI flag + gate coverage, 3 scenes PASS). Static review was INCONCLUSIVE -- it reads flat at honest 64km playable scale and the review scenes are fly-only, so walkability can't be judged from them. SCOPE DECISION (human-confirmed): integrate the oracle into live `demo.tscn` as a TOGGLEABLE candidate lane beside M2.3 so it can be flown AND walked, then compared. Anchor rationale: this integration work is SHARED with Approach C (the window-based GPU pipeline) -- candidate toggle, Rust->shader param plumbing, GLSL per-cell primitives, collision/biome contract -- so only the analytic sine-curve channels (~20%) are throwaway if C is later needed.

**RESOLVED (2026-06-07) — the oracle was integrated, walk-tested, and Branch 3 (escalate to C) was taken.** The per-cell oracle is now LIVE in `field_height.glsl` as a toggleable candidate (B key; `terrain_mode` 0=REFERENCE M2.3 / 1=SCAFFOLD_CANDIDATE oracle). All output gates green (`m2_4b_oracle_live_check` + full regression; std430 stayed 80 bytes via swap-pads). WALK-TEST FAILED visually: persistent dark crater/walls + flat/wrong terrain. ROOT CAUSE (measured by an LOD probe): the oracle is correctly spacing-independent, BUT its coarse-LOD max adjacent step is 1076m vs the M2.3 reference's 482m — the sharp analytic channel/carve features (1-smoothstep carves up to ~1km) alias catastrophically at coarse LOD, making ~1km vertical walls between cells/levels (the dark crater = mesh back-faces at the discontinuity). A per-cell field CANNOT do the Gaussian neighbor-smoothing that made the WG10 window-port read well — that's structurally why the per-cell approach fights the clipmap (same family as the M2.4a corduroy failure). DECISION (human-confirmed): ESCALATE to **Approach C** = window-based GPU pipeline (super-region apron + multi-pass blur + flow routing across page boundaries, seam-safe at every LOD; collision reads the multi-pass field). The oracle candidate lane STAYS (REFERENCE default = non-destructive; it's a useful per-cell base layer C builds neighbor passes on top of, and the toggle/param/collision plumbing is all reused). NEXT: brainstorm + spec Approach C as its own milestone-scale step BEFORE any C code. Specs: `docs/superpowers/specs/2026-06-06-m2-4b-oracle-validation-and-integration-design.md`; integration plan (done): `docs/superpowers/plans/2026-06-06-m2-4b-oracle-live-integration.md`; port map + hash decision: `docs/superpowers/plans/2026-06-06-m2-4b-oracle-port-map.md`.

**APPROACH C UNDERWAY (2026-06-07).** Brainstormed + specced + step 1 built. Spec: `docs/superpowers/specs/2026-06-07-m2-4c-macro-cache-terrain-design.md`. Architecture = TWO-LAYER field: a CACHED MACRO layer (bake the proven window-port's blur+flow per ~30km super-region OFF-frame, cache in RAM) + the existing per-cell detail. LIVE + PROCEDURAL (computed from seed on demand, cached; NOT a disk bake). Feasibility was MEASURED before committing: per-fine-page apron is infeasible (world-anchored ~5km blur halo = 441x overhead at 4m/cell); the macro bake is cheap (CPU 20-90ms off-frame @ 256/128m, reusing generate_seamsafe_fields verbatim); cache is small (~0.37MB/region @256m -> 450km prefetch ring ~84MB, so loading-screen prefetch is viable). Bake engine = CPU worker (the bake is off-frame so it never spends the ~8ms budget; GPU is a deferred swap behind the same cache/sample architecture if fast-flight latency ever bites). All tunable (engine-not-a-game). 4 build steps: (1) pure-Rust bake+cache [DONE], (2) GPU upload+sample bridge [NEXT — the one risky piece], (3) BakeScheduler+prefetch+never-black fallback, (4) loading-screen prefetch ring + terrain_mode wiring + human fly/walk visual gate. **STEP 1 DONE (test-gated):** `crate::macro_cache` (wg13) = RegionMacro/MacroBakeConfig, MacroBake::bake_region (reuses window-port, world-anchored apron, seamless tiling proven by a <1m adjacent-border test), bounded-LRU RegionCache. Workspace green (wg13 10 tests). Plan: `docs/superpowers/plans/2026-06-07-m2-4c-step1-macrobake-regioncache.md`. macro_cache has expected dead-code warnings until step 2/3 consume it. The M2.4b oracle candidate lane (terrain_mode 1, B key) STAYS as a non-destructive comparison/base; M2.3 REFERENCE is still the live default.

**STEP 2 (GPU MACRO BRIDGE) — COMPLETE (automated), PARKED FOR THE HUMAN WALK-TEST (2026-06-07).** The cached macro layer is now SAMPLED LIVE by field_height.glsl under `terrain_mode == 2` (MACRO_CACHE): `height = macro.height + macro_detail` — the LOD-stable smoothing the per-cell oracle couldn't do. All 6 tasks done, each through the full two-stage spec+quality review (subagent-driven, on main). Spec: `docs/superpowers/specs/2026-06-07-m2-4c-step2-gpu-macro-bridge-design.md`. Plan: `docs/superpowers/plans/2026-06-07-m2-4c-step2-gpu-macro-bridge.md`.
LOCKED DECISIONS (held): (1) 2x2 region binding — a page binds the up-to-4 regions it touches, shader selects per cell by world coord; infinite-safe, SELECT-not-blend (step-1 borders agree <1m). (2) R32F sampled textures + hardware bilinear (no 256m terracing). (3) terrain_mode 2, additive, non-destructive (REFERENCE default + oracle lane both STAY). (4) synchronous bake-on-demand in produce() behind `ensure_macro_neighborhood` — a SWAP SEAM so step 3 moves it off-thread without a rewrite (hitch into fresh regions accepted for step 2).
WHAT'S LIVE: GpuRegionMacro R32F textures + FieldGpu resident map (Task 2); PageParams 80->96 bytes 2x2 neighborhood + dispatch binds 12 samplers w/ a 1x1 placeholder for not-resident slots (Task 3); GLSL `macro_sample` (region-select + hardware-bilinear) + `macro_detail` (gentle +/-70m, NOT the oracle's sharp channels) + the mode-2 branch w/ composition_height never-black fallback (Task 4); page_pool produce() bakes+uploads the page's 2x2 block (`r0 = floor(origin/core_span)`, `macro_core_span` from `MacroBakeConfig::core_span_m()` so the UV mapping is EXACT), B key cycles REFERENCE->oracle->MACRO_CACHE (Task 5); `produce_macro_page` hook + gate `m2_4c_macro_live_check` (Task 6). The gdext 0.5.3 texture/sampler API is PINNED at the TOP OF `rust/gdext/src/macro_gpu.rs` (read it before touching GPU code).
GATE GREEN (agent-verified, own eyes): `m2_4c_macro_live_check` PASS — max_relief=1282.8m, interior max-step=12.1m, straddling(x-boundary) max-step=44.0m, longest_flat_run(both axes)=8 cells (thresholds relief>100, step<600, run<20): smoothed ranges, NO 256m terraces (hardware bilinear proven), bounded seam vs the oracle's 1076m walls. Full regression green (roundtrip/resident/m2_1/m2_2/m2_3/m1_7a/m2_4b_oracle_live); workspace 26 tests 0 failed; MODE 0/1 BIT-IDENTICAL (mode 2 additive/non-destructive). All pushed origin/main.
**NEXT = THE HUMAN WALK-TEST (the REAL gate — the agent did NOT self-certify the look).** Launch demo.tscn (`run.ps1`), press B twice to reach MACRO_CACHE, FLY + WALK. Judge: believable smoothed ranges/valleys + drainage? no terracing/walls? traversable? ACCEPT the one-time fly-into-fresh-region HITCH (synchronous bake = the accepted step-2 cost; step 3 fixes it). If the look passes -> step 2 CLOSES, start STEP 3 (BakeScheduler: off-thread bake + prefetch + never-black-while-baking). If not -> root-cause in the right layer, gated.
CARRY TO STEP 3 (from Task 5's code-quality review — design in, don't rediscover): (a) **RAM-cache vs GPU-resident-map lifetime divergence** — `RegionCache` is bounded-LRU (cap 256), evicts CPU data silently, but `FieldGpu::macro_resident` is uncapped + never told, so `evict_region` is never called -> orphaned GPU textures accumulate (~500KB/region, slow VRAM leak over a long flight). The BakeScheduler owns region lifecycle: have `RegionCache::insert` return the evicted key and call `gpu.evict_region`, or scan periodically. (b) **Wasted re-bake** — `ensure_macro_neighborhood` re-bakes (20-90ms) on RAM-miss even when the GPU still has it; step 3's async path should also guard on `gpu.has_region`. (c) minor: `set_macro_config` cap isn't live-resizable (next-init only). Editor closed for wg13 builds; GPU gates need --rendering-driver vulkan (no headless).
- **DEFERRED / KNOWN (don't rediscover):** (a) GPU page production async/double-buffer = workload-dependent, do at M2.6 perf pass. `max_eager_per_frame` is the live lever (RTX 3070 min target). (b) Character/collision controller polish is M2.7, not piecemeal: eye/slope geometry, jump feel, long-term HeightMapShape quality, and collision residency. Known-good now: collision tracks the active controller, radius 2, turbo substeps prevent tunneling. (c) Erosion (M6) carves AAA hydraulic realism into the composed macro later.
## 4. Known deferred items (don't "rediscover" these as bugs)
- **LOD transition seams** between clipmap levels (faint shading/detail steps as you fly) — roadmap-deferred **geomorph** polish. Not a crack, not z-fighting. Don't fix piecemeal (that's slop); it's a dedicated later pass. (Human confirmed 2026-06-06: still slightly present — fine to fix later.)
- **Far-edge streaming pop-in** (human-spotted 2026-06-06): at the loaded-world frontier, new pages can be seen *appearing* rather than dissolving into the depth fog as you fly outward. Distinct from the LOD detail-step (#above) and from the startup hitch (#below) — this is the *moving streaming frontier* showing through. Likely fog-vs-reach tuning (the coarsest edge isn't fully hidden) and/or pages produced inside the visible, non-fogged range under motion. Deferred polish (same family as async-load / fog tuning); M1's gate is "no black, no cracks" — both hold, this is far-edge LOD softness. Fix later, not piecemeal.
- **~150 ms one-time startup hitch** while the 6-level clipmap + GPU pipeline warm up — a *load* transient, not movement stutter. Proper fix is async page production / loading screen, later.
- **`show_page_tint`** debug checkerboard exists on `world_view` (default off).

## 5. The gotchas that will bite a fresh session (all in `01_TOOLCHAIN.md`, but here too)
- Build with `$env:CARGO_TARGET_DIR="D:\world gen 13\rust\target"` first, or the `.gdextension` won't find the dll (a global CARGO_TARGET_DIR points elsewhere).
- **GPU tests can't run `--headless`** (no RenderingDevice) — use `--rendering-driver vulkan`.
- **The open editor locks `wg13.dll`** → `cargo build` fails. Close it (or `run.ps1 -Stop`) before rebuilding. GDScript/shader-only changes need no rebuild.
- Godot exe: `C:\Godot\v4.6.2\Godot_v4.6.2-stable_mono_win64\Godot_v4.6.2-stable_mono_win64(_console).exe`. Use `_console` for stdout (gate PASS/FAIL); plain for the visible window.
- Commit messages: keep ASCII, write via a temp file (`git commit -F`) — here-strings with leading `/` or special chars have broken commits before.

## 6. How to recover "the feel" fast
- Skim the last 5 `DRIFT_LOG.md` entries — they show *how* recent problems were reasoned through (the annulus z-fight, the frame-time measurement lesson, the reverted eager-cap). That narrative IS the judgment to re-absorb.
- Run the 6 gates (one command loop in CODE_MAP) — green gates confirm the foundation is solid and you can trust it.
- Launch `run.ps1` and look. Seeing it working re-grounds you faster than reading.
- Then take the next gated step. Don't try to hold the whole project in your head — hold the *method* and the *current step*.
